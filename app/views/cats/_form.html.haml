= simple_form_for(@cat) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name, label: false, placeholder: '名前', input_html: {class: 'width-auto'}

  .form-actions
    = f.button :submit, class: 'btn btn-primary'
